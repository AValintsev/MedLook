@model IList<CategoryModel>
@if (Model.Count > 0)
{
	<div class="home-page-categories">
		@foreach (var item in Model)
		{
			<h2 class="title">
				<a href="@Url.RouteUrl("Category", new { SeName = item.SeName })" title="@item.Name">
					@item.Name
				</a>
			</h2>
		}
	</div>
}